Boilerplate
webpack + webpack-dev-server+ babel + react + react-hot-loader

also basic git
a. git init => for initializing git in repo
b. git status => for the status of the files
c. git add . => for adding all files to staging area
d. git commit -m "message" => for commiting changes to repo
for adding repo to remote(first create repo in the remote server)
e. git remote add origin 
https://github.com/ashishkkumar99/mild_boilerplate_react.git => add remote url in origin variable
d. git push -u origin master => pushed to master branch in remote repo